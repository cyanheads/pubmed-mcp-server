---
summary: Maintenance — `@cyanheads/mcp-ts-core` 0.7.0 → 0.7.5, `fast-xml-parser` 5.7.1 → 5.7.2. No runtime API changes for this server.
breaking: false
---

# 2.6.2 — 2026-04-26

Drop-in dependency bumps. Field-tested against the live NCBI surface to confirm the v2.6.1 inline-markup flatten path still survives the parser update (PMID 38785209 abstract continues to render `1.73 m²`).

## Changed

- **`@cyanheads/mcp-ts-core` 0.7.0 → 0.7.5** — picks up HTTP Origin guard fail-closed default, landing-page bearer validation, raw-payload removal from default request-completion logs, `format-parity` numeric normalization, and a `describe-on-fields` linter exemption for `z.literal` union variants. None of the affected runtime surfaces are used here, so adoption is transparent. MCP SDK pinned at `^1.29.0`.
- **`fast-xml-parser` 5.7.1 → 5.7.2** — stack-overflow fix on very long tag expressions ([NaturalIntelligence/fast-xml-parser#817](https://github.com/NaturalIntelligence/fast-xml-parser/issues/817)). Marginal hardening for deeply nested PMC full-text articles.

## Synced

- **Skills** from the framework: `api-linter` 1.1 → 1.2 (canonical form-client safety pattern), `maintenance` 1.5 → 1.6 (Step 4 surfaces new/changed skills explicitly), content refreshes for `field-test` and `design-mcp-server` (description leak audits). Mirrored into `.agents/skills/` and `.claude/skills/`.
- **Scripts** from the framework: `scripts/devcheck.ts` (now wires `Framework Antipatterns` as a check step) and the new `scripts/check-framework-antipatterns.ts` (no consumer-side findings).
- **`CLAUDE.md` / `AGENTS.md`** — form-client safety bullet now names the union+literal alternative for cases where regex/length constraints matter enough to surface in JSON Schema.

## Tests

- **466 passed** / 4 skipped. No suite changes — the dependency bumps are drop-in.
